build:
  gpu: false
  system_packages:
    - "ffmpeg"
  python_version: "3.11"
  python_requirements: "requirements.txt"  # Changed from python_packages
predict: "predict.py:Predictor"  # Added missing closing quote